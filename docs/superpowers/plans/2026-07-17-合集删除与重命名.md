# 合集删除与重命名命令 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 meme-pilot 现有 `/collection create` 基础上新增 `/collection delete <编号|名称>` 与 `/collection rename <旧编号|名称> <新名称>` 两个子命令，均无需确认、校验通过直接执行。

**Architecture:** 沿用现有 IndexManager Write Worker 写编排模型。在 `_WriteCoordinator._write_dispatch` 字典新增 `DELETE_COLLECTION`、`RENAME_COLLECTION` 两个执行器（guarded 包装，取得写锁后重检取消）。`MetadataStore` 新增 `rename_collection`（单事务改 name + 批量更新该合集所有 `meme.image_path` 首段 + 重建 frozen 缓存）；delete 复用现有 `delete_collection_and_reset_scopes`，空目录 `rmdir` 由协调器层在写锁内完成（先 rmdir 后删 DB，rmdir 失败 DB 未动）。门面 `IndexManager` 新增 `delete_collection`/`rename_collection` 薄入口，插件层 `collection.py` 按子命令派发。

**Tech Stack:** Python 3.12、NoneBot2、sqlite3（标准库）、`dataclasses`（frozen+slots）、`asyncio`、pytest + pytest-asyncio。LSP/类型检查用 `ty`，lint/格式化用 `ruff`。

## Global Constraints

- 项目 Python 3.12，**不使用** `from __future__ import annotations`（已原生支持类型标注延迟求值）。
- Python 函数使用 Google 风格 docstring，内容用**中文**；函数参数、返回值需类型标注；保持现有中文注释与用户提示风格。
- 文本搜索用 `rg`、文件查找用 `fd`（不可用回退 `find`）、语义查询优先 `codegraph`/LSP——本计划涉及精确文件编辑，直接用 Edit/Read。
- Python LSP 与类型检查用 `ty`；Linter 与格式化用 `ruff`。
- **严禁在 main 分支自行 `git add`/`git commit`/`git merge`**——本计划的每个 commit 步骤需用户审核后再执行；实现时可在 feature 分支提交，或累积改动等用户统一审核。提交信息用项目现有风格（`feat:`/`refactor:` 前缀，中文描述）。
- `MemeEntry` 是 `@dataclass(frozen=True, slots=True)`，不可原地修改字段，需用 `dataclasses.replace` 重建。
- `_WriteRequest` 是 `@dataclass(frozen=True, slots=True)`；新增字段必须有默认值，不能破坏现有构造调用。
- 插件层命令属组 A（仅私聊）；群聊 @bot 回复"此命令仅限私聊使用"；非授权用户静默忽略并记日志。
- 命令**无需确认**：不走 `got("confirm")`、不创建 selection/timeout task；插件层是"校验 → 调 IndexManager → 回复"单段处理，与现有 `create` 同构。
- 参考已获批 spec：`docs/superpowers/specs/2026-07-17-合集删除与重命名-design.md`。

---

## File Structure

| 文件 | 责任 | 本计划改动 |
|---|---|---|
| `bot/engine/metadata_store.py` | sqlite 元数据存储 + 内存缓存 | 新增 `rename_collection` 方法 |
| `bot/index_manager/index_types.py` | IndexManager 的异常/枚举/dataclass/结果类型 | 新增 2 个 WriteOp、2 个 `_WriteRequest` 字段、3 个异常类、2 个结果类 |
| `bot/index_manager/write_coordinator.py` | 写入编排：worker 循环 + 字典派发 + 各执行器 + 补偿 | `_write_dispatch` 新增 2 执行器 + `_execute_delete_collection` + `_execute_rename_collection`（含 guarded 包装与补偿） |
| `bot/index_manager/manager.py` | IndexManager 薄门面 | 新增 `delete_collection`/`rename_collection` 薄入口 |
| `bot/index_manager/__init__.py` | 子包 re-export | `__all__` 与 import 补新符号 |
| `bot/plugins/collection.py` | `/collection` 命令插件 | `handle_collection` 按子命令派发 create/delete/rename；新增错误映射与回复文案 |
| `bot/plugins/_help_text.py` | 帮助文本常量 | `HELP_TEXT` 扩三行 |
| `bot/plugins/plain_text.py` | 兜底消息 | 无需改动（已用 `help_text_for` 引用 `HELP_TEXT`，自动同步） |
| `docs/PRD.md` | 产品需求文档 | 3.3 扩三子节、3.5 帮助、分组表、边界情况 |
| `CONTEXT.md` | 术语表 | `/collection` 条目扩三条 + 新术语 |
| `README.md` | 部署与命令说明 | 命令清单同步（若有） |
| `tests/unit/engine/test_metadata_store.py` | MetadataStore 单元测试 | 新增 `rename_collection` 用例 |
| `tests/unit/engine/test_index_manager_delete_collection.py`（新建） | delete_collection 协调器/门面测试 | 新建文件 |
| `tests/unit/engine/test_index_manager_rename_collection.py`（新建） | rename_collection 协调器/门面测试 | 新建文件 |
| `tests/unit/plugins/test_collection.py` | `/collection` 插件测试 | 新增 delete/rename 用例 + 调整现有 `test_invalid_subcommand_replies_usage` |
| `tests/unit/plugins/test_help.py` | 帮助文本测试 | 新增 delete/rename 两行断言 |

**测试布局注意**：engine 测试在 `tests/unit/engine/`（无 `plugins` 子目录），插件测试在 `tests/unit/plugins/`。沿用现有命名 `test_index_manager_<feature>.py`。

---

### Task 1: MetadataStore.rename_collection

**Files:**
- Modify: `bot/engine/metadata_store.py`（在 `delete_collection_and_reset_scopes` 方法之后、`add` 方法之前，约 662 行处插入）
- Test: `tests/unit/engine/test_metadata_store.py`（新增用例）

**Interfaces:**
- Consumes: `self._id_to_collections`、`self._collection_name_to_id`、`self._entries`、`self._entries_by_collection`、`self._require_conn()`、`self._lock`；`MemeCollection`、`MemeEntry`（frozen）、`dataclasses.replace`。
- Produces: `rename_collection(self, collection_id: int, new_name: str) -> MemeCollection`；抛 `ValueError`（合集不存在或编号≤0）、`sqlite3.IntegrityError`（新名重复，由 DB UNIQUE 兜底）。后续 Task 3/4 的协调器调用本方法。

**实现要点（写代码前必读）**：
- `MemeEntry` 是 frozen dataclass，`collection_name` 字段在构造时由 `_make_entry` 通过 `self._collection_name(collection_id)` 填充（读 `_id_to_collections[cid].name`）。所以**必须先更新 `_id_to_collections[cid]` 的 name，再用 `dataclasses.replace(entry, image_path=new_path, collection_name=new_name)` 重建条目**，否则 `collection_name` 会停留在旧名。
- `image_path` 形如 `<旧名>/可能嵌套/x.webp`；首段替换为 `<新名>`。用 `entry.image_path.split("/", 1)` 取首段与余部，`new_path = new_name + "/" + rest`。根目录图片（无 `/`）不属于普通合集，不受影响——但本方法只处理 `collection_id > 0` 的合集条目，其 `image_path` 必然以 `<旧名>/` 开头。
- 事务结构：`UPDATE meme_collection SET name=? WHERE id=?` → 逐条 `UPDATE meme SET image_path=? WHERE id=?` → `conn.commit()`；任何异常 `conn.rollback()` 并抛出，**缓存不动**。commit 成功后再更新缓存。
- 缓存更新顺序：先 `_id_to_collections[cid] = MemeCollection(cid, new_name)` 与 `_collection_name_to_id`（删旧名 key、加新名）；再遍历 `_entries_by_collection.get(cid, {})` 的 entry_id，对 `_entries[eid]` 做 `replace` 并写回。

- [ ] **Step 1: 写失败测试**

在 `tests/unit/engine/test_metadata_store.py` 末尾新增（先读该文件确认现有 import 与 fixture 风格，复用其 `metadata_store` fixture；若该 fixture 名不同，用实际名）：

```python
import dataclasses
import sqlite3

from bot.engine.metadata_store import MetadataStore
from bot.engine.types import MemeCollection


def test_rename_collection_updates_name_image_path_and_cache(
    metadata_store: MetadataStore,
) -> None:
    """rename_collection 同步更新合集名、该合集条目 image_path 首段与全部缓存。"""
    collection = metadata_store.create_collection("新三国")
    cid = collection.id
    entry_id = metadata_store.add(
        "新三国/截图/a.webp", "加班", collection_id=cid
    )
    other_id = metadata_store.create_collection("甄嬛传")
    metadata_store.add("甄嬛传/b.webp", "别的", collection_id=other_id.id)

    renamed = metadata_store.rename_collection(cid, "旧三国")

    assert renamed == MemeCollection(cid, "旧三国")
    # meme_collection 行
    assert metadata_store.get_collection(cid) == MemeCollection(cid, "旧三国")
    assert metadata_store.get_collection_by_name("新三国") is None
    assert metadata_store.get_collection_by_name("旧三国") is not None
    # 条目 image_path 首段替换，id 与 local_id 不变
    entry = metadata_store.get_entry(entry_id)
    assert entry is not None
    assert entry.image_path == "旧三国/截图/a.webp"
    assert entry.collection_name == "旧三国"
    assert entry.collection_id == cid
    # 其他合集条目不受影响
    other = metadata_store.get_entry_by_public_id(
        MemePublicId(other_id.id, 1)
    )
    assert other is not None
    assert other.image_path == "甄嬛传/b.webp"
    assert other.collection_name == "甄嬛传"
    # public_id 仍可解析到同一条目
    assert (
        metadata_store.get_entry_by_public_id(MemePublicId(cid, 1)) is not None
    )
```

注：`MemePublicId` 需在文件顶部 import（`from bot.engine.types import MemePublicId`）。若该文件已 import 则不重复。

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/unit/engine/test_metadata_store.py::test_rename_collection_updates_name_image_path_and_cache -v`
Expected: FAIL with `AttributeError: 'MetadataStore' object has no attribute 'rename_collection'`

- [ ] **Step 3: 实现 `rename_collection`**

在 `bot/engine/metadata_store.py` 的 `delete_collection_and_reset_scopes` 方法之后（约 662 行，`@timed(logger, "MetadataStore 添加")` 装饰的 `add` 方法之前）插入：

```python
    def rename_collection(self, collection_id: int, new_name: str) -> MemeCollection:
        """重命名普通合集，并同步更新该合集所有条目的 image_path 首段。

        在单个 SQLite 事务内修改 ``meme_collection.name`` 与该合集每条
        ``meme.image_path`` 的首段（目录名）；提交成功后再更新内存缓存。
        任何异常回滚事务且缓存不动。

        Args:
            collection_id: 要重命名的普通合集编号；必须为正整数。
            new_name: 已通过领域校验的新合集名称。

        Returns:
            重命名后的合集快照（新名、同编号）。

        Raises:
            ValueError: 编号非正整数或合集不存在。
            sqlite3.IntegrityError: 新名称已被其他合集使用（DB UNIQUE 兜底）。
        """
        with self._lock:
            conn = self._require_conn()
            if collection_id <= 0:
                raise ValueError("collection_id 必须为正整数")
            collection = self._id_to_collections.get(collection_id)
            if collection is None:
                raise ValueError(f"collection_id={collection_id} 不存在")
            old_name = collection.name
            if new_name == old_name:
                return collection

            # 收集需要改 image_path 首段的条目（按 entry_id 升序，稳定）
            entry_ids = sorted(
                self._entries_by_collection.get(collection_id, {}).values()
            )
            updates: list[tuple[int, str]] = []
            for eid in entry_ids:
                entry = self._entries[eid]
                head, sep, rest = entry.image_path.partition("/")
                if not sep:
                    # 普通合集条目 image_path 必以 <合集名>/ 开头；兜底跳过
                    continue
                updates.append((eid, f"{new_name}/{rest}"))

            try:
                cursor = conn.execute(
                    "UPDATE meme_collection SET name = ? WHERE id = ?",
                    (new_name, collection_id),
                )
                if cursor.rowcount != 1:
                    raise sqlite3.DatabaseError("重命名合集必须影响 1 行")
                for eid, new_path in updates:
                    conn.execute(
                        "UPDATE meme SET image_path = ? WHERE id = ?",
                        (new_path, eid),
                    )
                conn.commit()
            except Exception:
                conn.rollback()
                raise

            # 提交成功后更新缓存：先合集名，再重建 frozen 条目
            renamed = MemeCollection(collection_id, new_name)
            self._id_to_collections[collection_id] = renamed
            self._collection_name_to_id.pop(old_name, None)
            self._collection_name_to_id[new_name] = collection_id
            for eid, new_path in updates:
                old_entry = self._entries[eid]
                self._entries[eid] = dataclasses.replace(
                    old_entry,
                    image_path=new_path,
                    collection_name=new_name,
                )
            return renamed
```

文件顶部若未 import `dataclasses`，添加 `import dataclasses`（确认现有 import 区）。

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/unit/engine/test_metadata_store.py::test_rename_collection_updates_name_image_path_and_cache -v`
Expected: PASS

- [ ] **Step 5: 补充边界测试**

在 `tests/unit/engine/test_metadata_store.py` 继续新增：

```python
def test_rename_collection_rejects_duplicate_name(
    metadata_store: MetadataStore,
) -> None:
    """重命名为已登记名称时回滚，DB 与缓存不变。"""
    first = metadata_store.create_collection("一")
    second = metadata_store.create_collection("二")
    metadata_store.add("二/a.webp", "文字", collection_id=second.id)

    with pytest.raises(sqlite3.IntegrityError):
        metadata_store.rename_collection(second.id, "一")

    # 缓存与 DB 均未变
    assert metadata_store.get_collection(second.id) == MemeCollection(second.id, "二")
    assert metadata_store.get_collection_by_name("一") == MemeCollection(first.id, "一")
    entry = metadata_store.get_entry_by_public_id(MemePublicId(second.id, 1))
    assert entry is not None
    assert entry.image_path == "二/a.webp"
    assert entry.collection_name == "二"


def test_rename_collection_rejects_unknown_id(metadata_store: MetadataStore) -> None:
    """不存在的合集编号抛 ValueError。"""
    with pytest.raises(ValueError):
        metadata_store.rename_collection(999, "新名")


def test_rename_collection_rejects_zero_id(metadata_store: MetadataStore) -> None:
    """编号 0（全局）抛 ValueError。"""
    with pytest.raises(ValueError):
        metadata_store.rename_collection(0, "新名")


def test_rename_collection_same_name_is_noop(metadata_store: MetadataStore) -> None:
    """新名等于旧名时直接返回，不触发任何 SQL 写入。"""
    collection = metadata_store.create_collection("同名")
    result = metadata_store.rename_collection(collection.id, "同名")
    assert result == collection
```

Run: `uv run pytest tests/unit/engine/test_metadata_store.py -v -k rename_collection`
Expected: 全部 PASS

- [ ] **Step 6: 类型检查与 lint**

Run: `uv run ty check bot/engine/metadata_store.py` 和 `uv run ruff check bot/engine/metadata_store.py tests/unit/engine/test_metadata_store.py`
Expected: 无错误（ty 清零，ruff 无告警）。如有 import 顺序告警按 ruff 提示修。

- [ ] **Step 7: 提交（需用户审核）**

```bash
git add bot/engine/metadata_store.py tests/unit/engine/test_metadata_store.py
git commit -m "feat(metadata_store): 新增 rename_collection 同步改合集名与 image_path 首段"
```

若在 main 分支，**先切 feature 分支**：`git switch -c feat/collection-delete-rename`。提交前请用户审核。

---

### Task 2: index_types 新增 WriteOp/请求字段/异常/结果类型

**Files:**
- Modify: `bot/index_manager/index_types.py`
- Modify: `bot/index_manager/__init__.py`（re-export）

**Interfaces:**
- Consumes: `MemeCollection`（已 import 自 `bot.engine.types`）、`dataclass`/`field`/`Enum`/`auto`（已 import）。
- Produces: `WriteOp.DELETE_COLLECTION`、`WriteOp.RENAME_COLLECTION`；`_WriteRequest.collection_id`、`_WriteRequest.new_collection_name`；`CollectionNotEmptyError`、`CollectionDeleteError`、`CollectionRenameTargetExistsError`；`DeleteCollectionResult`、`RenameCollectionResult`。供 Task 3/4 协调器与 Task 5 门面、Task 6 插件使用。

- [ ] **Step 1: 扩展 WriteOp 枚举**

在 `bot/index_manager/index_types.py` 的 `WriteOp` 类里（约 119-128 行），在 `CREATE_COLLECTION = auto()` 之后新增：

```python
    DELETE_COLLECTION = auto()
    RENAME_COLLECTION = auto()
```

- [ ] **Step 2: 给 `_WriteRequest` 新增字段**

在 `_WriteRequest` dataclass（约 131-174 行）的字段区，`collection_name: str = ""` 之后新增两个字段：

```python
    collection_id: int = 0
    new_collection_name: str = ""
```

注意：`collection_name` 字段已存在（CREATE_COLLECTION 用）；新增的 `collection_id` 与 `new_collection_name` 给 delete/rename 用。同时更新 `_WriteRequest` 的 docstring Attributes 列表，补两行：

```python
        collection_id: DELETE_COLLECTION 时的目标合集编号；RENAME_COLLECTION 时为源合集编号。
        new_collection_name: RENAME_COLLECTION 时已通过领域校验的新合集名称。
```

- [ ] **Step 3: 新增 3 个异常类**

在 `CollectionCreateError` 类之后（约 78-79 行之后）新增：

```python
class CollectionNotEmptyError(RuntimeError):
    """待删除合集仍含表情包，不能删除。"""


class CollectionDeleteError(RuntimeError):
    """删除合集失败且可能需要人工检查文件系统。"""


class CollectionRenameTargetExistsError(RuntimeError):
    """重命名目标名称已被其他合集使用。"""

    def __init__(self, collection: MemeCollection) -> None:
        """初始化重命名目标重名错误。

        Args:
            collection: 已占用目标名称的合集快照。
        """
        self.collection = collection
        super().__init__(f"合集名称已存在: {collection.id}:{collection.name}")
```

- [ ] **Step 4: 新增 2 个结果类**

在 `CreateCollectionResult` dataclass 之后（约 91 行之后）新增：

```python
@dataclass(frozen=True, slots=True)
class DeleteCollectionResult:
    """删除合集后的持久状态快照。

    Attributes:
        collection: 已删除的合集快照（用于回复编号与名称）。
        reset_scope_count: 回退到全部合集的 ChatScope 行数。
    """

    collection: MemeCollection
    reset_scope_count: int


@dataclass(frozen=True, slots=True)
class RenameCollectionResult:
    """重命名合集后的持久状态快照。

    Attributes:
        collection: 重命名后的合集快照（新名、同编号）。
        old_name: 重命名前的名称。
        new_name: 重命名后的名称。
        entry_count: 受 image_path 首段变更影响的条目数。
    """

    collection: MemeCollection
    old_name: str
    new_name: str
    entry_count: int
```

- [ ] **Step 5: 更新 `__init__.py` re-export**

在 `bot/index_manager/__init__.py` 的 `from .index_types import (...)` 块里，按字母序插入新符号：

```python
    CollectionDeleteError,
    CollectionNotEmptyError,
    CollectionRenameTargetExistsError,
    DeleteCollectionResult,
    RenameCollectionResult,
```

并在 `__all__` 列表里同步加入这 5 个名字（按现有字母序位置插入）。

- [ ] **Step 6: 类型检查与 lint**

Run: `uv run ty check bot/index_manager/index_types.py bot/index_manager/__init__.py` 和 `uv run ruff check bot/index_manager/index_types.py bot/index_manager/__init__.py`
Expected: 无错误。

- [ ] **Step 7: 冒烟测试确认未破坏现有导入**

Run: `uv run pytest tests/unit/engine/test_index_manager_create_collection.py -v`
Expected: 全部 PASS（确认 `_WriteRequest` 新字段不破坏现有 create 测试）。

- [ ] **Step 8: 提交（需用户审核）**

```bash
git add bot/index_manager/index_types.py bot/index_manager/__init__.py
git commit -m "feat(index_types): 新增 delete/rename 合集的 WriteOp、请求字段、异常与结果类型"
```

---

### Task 3: 协调器 delete_collection 执行器 + 门面入口

**Files:**
- Modify: `bot/index_manager/write_coordinator.py`（`_write_dispatch` 字典 + 新增 `_execute_delete_collection` + guarded 包装）
- Modify: `bot/index_manager/manager.py`（新增 `delete_collection` 薄入口）
- Test: `tests/unit/engine/test_index_manager_delete_collection.py`（新建）

**Interfaces:**
- Consumes（from Task 1/2）: `MetadataStore.get_collection`、`MetadataStore.collection_entry_count`、`MetadataStore.delete_collection_and_reset_scopes`；`WriteOp.DELETE_COLLECTION`、`_WriteRequest.collection_id`、`CollectionNotFoundError`（自 `bot.engine.collection_manager`）、`CollectionNotEmptyError`、`CollectionDeleteError`、`CollectionPathConflictError`、`DeleteCollectionResult`；现有 `_get_collection_directory_identity`。
- Consumes（门面）: `CollectionManager.resolve_collection`（编号优先、名称兜底，已存在）。
- Produces: `_WriteCoordinator._execute_delete_collection` + `_execute_delete_collection_guarded`；`IndexManager.delete_collection(raw: str) -> DeleteCollectionResult`。供 Task 6 插件层调用。

**实现要点**：
- **先 rmdir、后删 DB**：rmdir 失败时 DB 未动，无需回滚（符合 spec 决策"回滚 DB 恢复合集"——实际更干净，根本没动 DB）。
- 写锁内二次空目录校验：DB 层 `collection_entry_count` 判空后，`os.scandir(target)` 复核目录为空（防 DB 判空与 rmdir 之间并发写入）；非空抛 `CollectionDeleteError`。
- 删 DB 失败补偿：目录已删、DB 还在 → `target.mkdir()` 恢复空目录，记 `logger.critical`，抛 `CollectionDeleteError`。
- guarded 包装与 `_execute_create_collection_guarded` 同构：取得写锁后重检 `req.future.done()`，已取消返回 None。

- [ ] **Step 1: 写失败测试（新建文件）**

创建 `tests/unit/engine/test_index_manager_delete_collection.py`，先写 fixture（复用 `test_index_manager_create_collection.py` 的 fixture 模式）：

```python
"""IndexManager 删除合集测试。"""

import asyncio
import logging
import os
from collections.abc import AsyncGenerator
from pathlib import Path
from typing import cast
from unittest.mock import MagicMock

import pytest
import pytest_asyncio

from bot.engine.collection_manager import CollectionNotFoundError
from bot.engine.metadata_store import MetadataStore
from bot.engine.types import MemeCollection
from bot.engine.vector_store import VectorStore
from bot.index_manager import (
    CollectionDeleteError,
    CollectionNotEmptyError,
    IndexAddCancelledError,
    IndexManager,
    RefreshInProgressError,
)
from bot.session import ChatScope


@pytest_asyncio.fixture
async def index_manager(
    tmp_path: Path,
) -> AsyncGenerator[IndexManager, None]:
    """创建使用真实 SQLite 与临时目录的 IndexManager。"""
    memes_dir = tmp_path / "memes"
    memes_dir.mkdir()
    metadata_store = MetadataStore(str(tmp_path / "data" / "index.db"))
    metadata_store.load()
    vector_store = cast(VectorStore, MagicMock(spec=VectorStore))
    manager = IndexManager(
        metadata_store=metadata_store,
        vector_store=vector_store,
        memes_dir=str(memes_dir),
    )
    yield manager
    await manager.close()


@pytest.mark.asyncio
async def test_delete_collection_removes_directory_db_and_resets_scope(
    index_manager: IndexManager,
) -> None:
    """删除空合集：rmdir 空目录 + 删 DB 记录 + 回退引用它的 ChatScope。"""
    result = await index_manager.create_collection("新三国")
    cid = result.collection.id
    target = index_manager._memes_dir / "新三国"
    assert target.is_dir()
    scope = ChatScope(user_id=1, chat_type="private", chat_id=1)
    index_manager._metadata_store.set_selected_collection(scope, cid)

    deleted = await index_manager.delete_collection("新三国")

    assert deleted.collection == MemeCollection(cid, "新三国")
    assert deleted.reset_scope_count == 1
    assert not target.exists()
    assert index_manager._metadata_store.get_collection(cid) is None
    assert index_manager._metadata_store.get_selected_collection(scope) == 0


@pytest.mark.asyncio
async def test_delete_collection_accepts_numeric_id(index_manager: IndexManager) -> None:
    """按编号删除合集。"""
    result = await index_manager.create_collection("编号合集")
    deleted = await index_manager.delete_collection(str(result.collection.id))
    assert deleted.collection.id == result.collection.id
    assert index_manager._metadata_store.get_collection(result.collection.id) is None


@pytest.mark.asyncio
async def test_delete_collection_rejects_non_empty(
    index_manager: IndexManager,
) -> None:
    """非空合集拒绝删除，DB 与目录都不动。"""
    result = await index_manager.create_collection("非空")
    cid = result.collection.id
    target = index_manager._memes_dir / "非空"
    (target / "a.webp").write_bytes(b"x")
    index_manager._metadata_store.add("非空/a.webp", "文字", collection_id=cid)

    with pytest.raises(CollectionNotEmptyError):
        await index_manager.delete_collection("非空")

    assert target.is_dir()
    assert index_manager._metadata_store.get_collection(cid) is not None


@pytest.mark.asyncio
async def test_delete_collection_rejects_unknown(index_manager: IndexManager) -> None:
    """不存在的合集抛 CollectionNotFoundError。"""
    with pytest.raises(CollectionNotFoundError):
        await index_manager.delete_collection("不存在")


@pytest.mark.asyncio
async def test_delete_collection_rmdir_failure_leaves_db_intact(
    index_manager: IndexManager,
    monkeypatch: pytest.MonkeyPatch,
) -> None:
    """rmdir 失败时 DB 未动，抛 CollectionDeleteError。"""
    result = await index_manager.create_collection("rmdir失败")
    cid = result.collection.id
    target = index_manager._memes_dir / "rmdir失败"

    def fail_rmdir(self: Path) -> None:
        raise OSError("rmdir failed")

    monkeypatch.setattr(Path, "rmdir", fail_rmdir)

    with pytest.raises(CollectionDeleteError):
        await index_manager.delete_collection("rmdir失败")

    assert target.is_dir()
    assert index_manager._metadata_store.get_collection(cid) is not None


@pytest.mark.asyncio
async def test_delete_collection_db_failure_restores_directory(
    index_manager: IndexManager,
    monkeypatch: pytest.MonkeyPatch,
    caplog: pytest.LogCaptureFixture,
) -> None:
    """删 DB 失败时补偿恢复空目录，记 critical 日志，抛 CollectionDeleteError。"""
    result = await index_manager.create_collection("db失败")
    cid = result.collection.id
    target = index_manager._memes_dir / "db失败"

    def fail_delete(cid_arg: int) -> int:
        raise RuntimeError("db unavailable")

    monkeypatch.setattr(
        index_manager._metadata_store,
        "delete_collection_and_reset_scopes",
        fail_delete,
    )
    caplog.set_level(logging.CRITICAL, logger="bot.index_manager.write_coordinator")

    with pytest.raises(CollectionDeleteError):
        await index_manager.delete_collection("db失败")

    assert target.is_dir()
    assert index_manager._metadata_store.get_collection(cid) is not None
    assert any(
        record.levelno == logging.CRITICAL for record in caplog.records
    )


@pytest.mark.asyncio
async def test_delete_collection_rejected_while_refresh_active(
    index_manager: IndexManager,
) -> None:
    index_manager._refresh_active = True
    await index_manager.create_collection("刷新中")
    with pytest.raises(RefreshInProgressError):
        await index_manager.delete_collection("刷新中")


@pytest.mark.asyncio
async def test_delete_collection_rejected_while_shutting_down(
    index_manager: IndexManager,
) -> None:
    index_manager._shutting_down = True
    with pytest.raises(IndexAddCancelledError, match="Bot 正在关闭"):
        await index_manager.delete_collection("关闭中")
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/unit/engine/test_index_manager_delete_collection.py -v`
Expected: 多数 FAIL with `AttributeError: 'IndexManager' object has no attribute 'delete_collection'`

- [ ] **Step 3: 实现协调器 `_execute_delete_collection` + guarded 包装**

在 `bot/index_manager/write_coordinator.py` 的 `_execute_create_collection_guarded` 方法之后（约 474 行后），新增两个方法。先确认文件顶部 import 已含 `DeleteCollectionResult`、`CollectionDeleteError`、`CollectionNotEmptyError`（Task 2 已在 `from .index_types import (...)` 块加入；若未加入需补）。还需 `CollectionNotFoundError`——检查 import，该文件第 19 行已 `from bot.engine.collection_manager import CollectionNotFoundError, validate_collection_name`。

```python
    async def _execute_delete_collection_guarded(
        self, req: _WriteRequest
    ) -> DeleteCollectionResult | None:
        """写锁内执行 DELETE_COLLECTION：取得写锁后重检取消再删除合集。

        Args:
            req: 写入任务单元（collection_id 已解析）。

        Returns:
            删除结果；等待写锁期间请求已被取消时返回 None（外层不 set_result）。
        """
        if req.future.done():
            return None
        return await self._execute_delete_collection(req)

    async def _execute_delete_collection(
        self, req: _WriteRequest
    ) -> DeleteCollectionResult:
        """写锁内删除空合集：先 rmdir 空目录、后删 DB（rmdir 失败 DB 未动）。

        Args:
            req: 写入任务单元，含目标合集编号。

        Returns:
            删除结果，含回退到全部合集的 ChatScope 行数。

        Raises:
            CollectionNotFoundError: 合集不存在。
            CollectionNotEmptyError: 合集仍含表情包。
            CollectionPathConflictError: 同名路径不是普通目录。
            CollectionDeleteError: rmdir 失败或删 DB 失败且补偿失败。
        """
        collection = await asyncio.to_thread(
            self._metadata_store.get_collection, req.collection_id
        )
        if collection is None:
            raise CollectionNotFoundError(str(req.collection_id))

        if (
            await asyncio.to_thread(
                self._metadata_store.collection_entry_count, req.collection_id
            )
            > 0
        ):
            raise CollectionNotEmptyError(
                f"合集 {req.collection_id} 仍含表情包，不能删除"
            )

        target = self._memes_dir / collection.name
        # 目录身份校验：非普通目录（文件/符号链接/不存在）拒绝
        await asyncio.to_thread(self._get_collection_directory_identity, target)
        # 写锁内二次复核目录为空，防 DB 判空与 rmdir 之间并发写入
        try:
            with os.scandir(target) as entries:
                if any(True for _ in entries):
                    raise CollectionDeleteError(
                        f"合集目录非空，无法删除: {collection.name}"
                    )
        except OSError as exc:
            raise CollectionDeleteError(
                f"检查合集目录失败: {collection.name}"
            ) from exc

        try:
            target.rmdir()
        except OSError as exc:
            raise CollectionDeleteError(
                f"删除合集目录失败: {collection.name}"
            ) from exc

        try:
            reset_count = await asyncio.to_thread(
                self._metadata_store.delete_collection_and_reset_scopes,
                req.collection_id,
            )
        except Exception as exc:
            # 目录已删、DB 删除失败：补偿恢复空目录
            try:
                target.mkdir()
            except OSError as restore_exc:
                logger.critical(
                    "删除合集 DB 失败且目录恢复失败: id=%d, name=%r, error=%s",
                    req.collection_id,
                    collection.name,
                    restore_exc,
                    exc_info=True,
                )
            else:
                logger.critical(
                    "删除合集 DB 失败，已恢复空目录: id=%d, name=%r, error=%s",
                    req.collection_id,
                    collection.name,
                    exc,
                    exc_info=True,
                )
            raise CollectionDeleteError(
                f"删除合集 DB 失败: {collection.name}"
            ) from exc

        logger.info(
            "合集删除完成: id=%d, name=%r, reset_scopes=%d",
            req.collection_id,
            collection.name,
            reset_count,
        )
        return DeleteCollectionResult(
            collection=collection,
            reset_scope_count=reset_count,
        )
```

注意：`_get_collection_directory_identity` 是 `@staticmethod`，通过 `self._get_collection_directory_identity(target)` 调用即可（与 create 执行器一致）。

- [ ] **Step 4: 注册到 `_write_dispatch` 字典**

在 `_WriteCoordinator.__init__` 的 `self._write_dispatch` 字典（约 124-134 行）里，`WriteOp.CREATE_COLLECTION: self._execute_create_collection_guarded,` 之后新增：

```python
            WriteOp.DELETE_COLLECTION: self._execute_delete_collection_guarded,
```

- [ ] **Step 5: 实现门面 `IndexManager.delete_collection`**

在 `bot/index_manager/manager.py` 的 `create_collection` 方法之后（约 740 行后），新增。先确认顶部 import 已含 `DeleteCollectionResult`（Task 2 已在 `from .index_types import (...)` 块加入；`CollectionNotFoundError` 在 `prepare_move` 内部局部 import 过，这里在方法内局部 import 保持一致）：

```python
    async def delete_collection(self, raw: str) -> DeleteCollectionResult:
        """删除空合集：rmdir 空目录 + 删 DB 记录 + 回退引用它的 ChatScope。

        Args:
            raw: 目标合集编号或精确名称（编号优先、名称兜底）。

        Returns:
            删除结果，含回退到全部合集的 ChatScope 行数。

        Raises:
            InvalidCollectionNameError: 不会触发（delete 不创建新名称）。
            CollectionNotFoundError: 目标合集不存在。
            CollectionNotEmptyError: 合集仍含表情包。
            CollectionPathConflictError: 同名路径不是普通目录。
            CollectionDeleteError: rmdir 或删 DB 失败且补偿失败。
            RefreshInProgressError: 索引刷新正在执行。
            IndexAddCancelledError: Bot 正在关闭或写入 worker 被取消。
        """
        if self._shutting_down:
            raise IndexAddCancelledError("Bot 正在关闭")
        if self._refresh_active:
            raise RefreshInProgressError("索引正在刷新，请稍后再试")

        from bot.engine.collection_manager import CollectionNotFoundError

        collection = await asyncio.to_thread(
            self._collection_manager.resolve_collection, raw
        )
        self._ensure_write_worker()
        loop = asyncio.get_running_loop()
        future: asyncio.Future[DeleteCollectionResult] = loop.create_future()
        req = _WriteRequest(
            op=WriteOp.DELETE_COLLECTION,
            future=future,  # type: ignore[arg-type]  # ty: ignore[invalid-argument-type]
            collection_id=collection.id,
        )
        return await self._coordinator.submit(req)
```

- [ ] **Step 6: 运行测试确认通过**

Run: `uv run pytest tests/unit/engine/test_index_manager_delete_collection.py -v`
Expected: 全部 PASS

- [ ] **Step 7: 类型检查与 lint**

Run: `uv run ty check bot/index_manager/write_coordinator.py bot/index_manager/manager.py` 和 `uv run ruff check bot/index_manager/write_coordinator.py bot/index_manager/manager.py tests/unit/engine/test_index_manager_delete_collection.py`
Expected: 无错误。

- [ ] **Step 8: 回归测试**

Run: `uv run pytest tests/unit/engine/test_index_manager_create_collection.py tests/unit/engine/test_index_manager_delete.py -v`
Expected: 全部 PASS（确认新增执行器与 dispatch 字典未破坏 create 与 meme delete）。

- [ ] **Step 9: 提交（需用户审核）**

```bash
git add bot/index_manager/write_coordinator.py bot/index_manager/manager.py tests/unit/engine/test_index_manager_delete_collection.py
git commit -m "feat(index_manager): 新增 delete_collection 写锁内先 rmdir 后删 DB"
```

---

### Task 4: 协调器 rename_collection 执行器 + 门面入口

**Files:**
- Modify: `bot/index_manager/write_coordinator.py`（`_write_dispatch` 字典 + 新增 `_execute_rename_collection` + guarded 包装）
- Modify: `bot/index_manager/manager.py`（新增 `rename_collection` 薄入口）
- Test: `tests/unit/engine/test_index_manager_rename_collection.py`（新建）

**Interfaces:**
- Consumes（from Task 1/2/3）: `MetadataStore.rename_collection`、`MetadataStore.get_collection`、`MetadataStore.get_collection_by_name`；`WriteOp.RENAME_COLLECTION`、`_WriteRequest.collection_id`、`_WriteRequest.new_collection_name`；`CollectionNotFoundError`、`CollectionRenameTargetExistsError`、`CollectionPathConflictError`、`CollectionCreateError`（补偿失败复用）、`RenameCollectionResult`；现有 `_get_collection_directory_identity`；`CollectionManager.resolve_collection`、`validate_collection_name`。
- Produces: `_WriteCoordinator._execute_rename_collection` + `_execute_rename_collection_guarded`；`IndexManager.rename_collection(raw: str, new_name: str) -> RenameCollectionResult`。供 Task 6 插件层调用。

**实现要点**：
- **先 SQLite、后文件 rename**：SQLite 改 name + image_path（Task 1 的 `rename_collection`），再 `old_dir.rename(new_dir)`。
- 目录身份与冲突校验：`old_dir` 用 `_get_collection_directory_identity`（不存在/非普通目录抛 `CollectionPathConflictError`）；`new_dir` 已存在抛 `CollectionPathConflictError`。
- 文件 rename 失败补偿：调 `metadata_store.rename_collection(cid, old_name)` 回滚 SQLite 与缓存；补偿失败记 `logger.critical`，抛 `CollectionCreateError`（复用，插件层映射为"合集重命名失败"）。
- `memes/` 内部同盘，`Path.rename` 安全（非跨 bind mount）。
- rename 不动 chroma（`collection_id` 不变），不影响 ChatScope（`collection_id` 不变）——测试断言。

- [ ] **Step 1: 写失败测试（新建文件）**

创建 `tests/unit/engine/test_index_manager_rename_collection.py`，fixture 与 Task 3 相同：

```python
"""IndexManager 重命名合集测试。"""

from collections.abc import AsyncGenerator
from pathlib import Path
from typing import cast
from unittest.mock import MagicMock

import pytest
import pytest_asyncio

from bot.engine.collection_manager import CollectionNotFoundError
from bot.engine.collection_manager import InvalidCollectionNameError
from bot.engine.metadata_store import MetadataStore
from bot.engine.types import MemeCollection, MemePublicId
from bot.engine.vector_store import VectorStore
from bot.index_manager import (
    CollectionCreateError,
    CollectionPathConflictError,
    CollectionRenameTargetExistsError,
    IndexAddCancelledError,
    IndexManager,
    RefreshInProgressError,
)
from bot.session import ChatScope


@pytest_asyncio.fixture
async def index_manager(
    tmp_path: Path,
) -> AsyncGenerator[IndexManager, None]:
    """创建使用真实 SQLite 与临时目录的 IndexManager。"""
    memes_dir = tmp_path / "memes"
    memes_dir.mkdir()
    metadata_store = MetadataStore(str(tmp_path / "data" / "index.db"))
    metadata_store.load()
    vector_store = cast(VectorStore, MagicMock(spec=VectorStore))
    manager = IndexManager(
        metadata_store=metadata_store,
        vector_store=vector_store,
        memes_dir=str(memes_dir),
    )
    yield manager
    await manager.close()


async def _seed_collection_with_entry(
    index_manager: IndexManager, name: str
) -> tuple[MemeCollection, int]:
    """创建合集并写入一条条目，返回合集与 entry_id。"""
    result = await index_manager.create_collection(name)
    cid = result.collection.id
    target = index_manager._memes_dir / name
    (target / "a.webp").write_bytes(b"x")
    eid = index_manager._metadata_store.add(
        f"{name}/a.webp", "文字", collection_id=cid
    )
    return result.collection, eid


@pytest.mark.asyncio
async def test_rename_collection_updates_db_dir_and_image_path(
    index_manager: IndexManager,
) -> None:
    """重命名：DB name + 目录重命名 + image_path 首段 + collection_id 不变。"""
    collection, eid = await _seed_collection_with_entry(index_manager, "新三国")
    cid = collection.id
    scope = ChatScope(user_id=1, chat_type="private", chat_id=1)
    index_manager._metadata_store.set_selected_collection(scope, cid)

    result = await index_manager.rename_collection("新三国", "旧三国")

    assert result.collection == MemeCollection(cid, "旧三国")
    assert result.old_name == "新三国"
    assert result.new_name == "旧三国"
    assert result.entry_count == 1
    # 目录已重命名
    assert not (index_manager._memes_dir / "新三国").exists()
    assert (index_manager._memes_dir / "旧三国").is_dir()
    # DB name 更新
    assert index_manager._metadata_store.get_collection(cid) == MemeCollection(
        cid, "旧三国"
    )
    # image_path 首段替换
    entry = index_manager._metadata_store.get_entry(eid)
    assert entry is not None
    assert entry.image_path == "旧三国/a.webp"
    assert entry.collection_id == cid
    # ChatScope 不受影响（collection_id 不变）
    assert index_manager._metadata_store.get_selected_collection(scope) == cid


@pytest.mark.asyncio
async def test_rename_collection_accepts_numeric_id(
    index_manager: IndexManager,
) -> None:
    """按编号重命名。"""
    collection, _ = await _seed_collection_with_entry(index_manager, "编号合集")
    await index_manager.rename_collection(str(collection.id), "新名")
    assert index_manager._metadata_store.get_collection(collection.id) == MemeCollection(
        collection.id, "新名"
    )


@pytest.mark.asyncio
async def test_rename_collection_rejects_duplicate_target(
    index_manager: IndexManager,
) -> None:
    """目标名已被其他合集使用时拒绝，DB 与目录都不动。"""
    await index_manager.create_collection("甄嬛传")
    collection, _ = await _seed_collection_with_entry(index_manager, "新三国")

    with pytest.raises(CollectionRenameTargetExistsError) as caught:
        await index_manager.rename_collection("新三国", "甄嬛传")

    assert caught.value.collection.name == "甄嬛传"
    assert (index_manager._memes_dir / "新三国").is_dir()
    assert index_manager._metadata_store.get_collection(collection.id) == MemeCollection(
        collection.id, "新三国"
    )


@pytest.mark.asyncio
async def test_rename_collection_rejects_existing_target_directory(
    index_manager: IndexManager,
) -> None:
    """目标名对应路径已存在（未登记目录）时拒绝。"""
    collection, _ = await _seed_collection_with_entry(index_manager, "新三国")
    (index_manager._memes_dir / "未登记目录").mkdir()

    with pytest.raises(CollectionPathConflictError):
        await index_manager.rename_collection("新三国", "未登记目录")

    assert (index_manager._memes_dir / "新三国").is_dir()
    assert index_manager._metadata_store.get_collection(collection.id) == MemeCollection(
        collection.id, "新三国"
    )


@pytest.mark.asyncio
async def test_rename_collection_rejects_unknown_source(
    index_manager: IndexManager,
) -> None:
    with pytest.raises(CollectionNotFoundError):
        await index_manager.rename_collection("不存在", "新名")


@pytest.mark.asyncio
async def test_rename_collection_rejects_invalid_new_name(
    index_manager: IndexManager,
) -> None:
    """新名称走 validate_collection_name 校验。"""
    await _seed_collection_with_entry(index_manager, "新三国")
    with pytest.raises(InvalidCollectionNameError):
        await index_manager.rename_collection("新三国", "全 局")


@pytest.mark.asyncio
async def test_rename_collection_dir_rename_failure_rolls_back_sqlite(
    index_manager: IndexManager,
    monkeypatch: pytest.MonkeyPatch,
    caplog: pytest.LogCaptureFixture,
) -> None:
    """目录 rename 失败时回滚 SQLite 与缓存，抛 CollectionCreateError。"""
    collection, eid = await _seed_collection_with_entry(index_manager, "新三国")
    cid = collection.id

    def fail_rename(self: Path, target: Path) -> None:
        raise OSError("rename failed")

    monkeypatch.setattr(Path, "rename", fail_rename)
    caplog.set_level(logging.CRITICAL, logger="bot.index_manager.write_coordinator")

    with pytest.raises(CollectionCreateError):
        await index_manager.rename_collection("新三国", "旧三国")

    # SQLite 回滚：合集名仍为旧名，image_path 仍为旧首段
    assert index_manager._metadata_store.get_collection(cid) == MemeCollection(
        cid, "新三国"
    )
    entry = index_manager._metadata_store.get_entry(eid)
    assert entry is not None
    assert entry.image_path == "新三国/a.webp"
```

注：`test_rename_collection_dir_rename_failure_rolls_back_sqlite` 用到 `logging`，需在文件顶部 `import logging`。

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/unit/engine/test_index_manager_rename_collection.py -v`
Expected: 多数 FAIL with `AttributeError: 'IndexManager' object has no attribute 'rename_collection'`

- [ ] **Step 3: 实现协调器 `_execute_rename_collection` + guarded 包装**

在 `bot/index_manager/write_coordinator.py` 的 `_execute_delete_collection` 方法之后（Task 3 新增的代码后），新增。先确认 `from .index_types import (...)` 块已含 `RenameCollectionResult`、`CollectionRenameTargetExistsError`（Task 2 已加）：

```python
    async def _execute_rename_collection_guarded(
        self, req: _WriteRequest
    ) -> RenameCollectionResult | None:
        """写锁内执行 RENAME_COLLECTION：取得写锁后重检取消再重命名。

        Args:
            req: 写入任务单元（collection_id 与 new_collection_name 已解析）。

        Returns:
            重命名结果；等待写锁期间请求已被取消时返回 None（外层不 set_result）。
        """
        if req.future.done():
            return None
        return await self._execute_rename_collection(req)

    async def _execute_rename_collection(
        self, req: _WriteRequest
    ) -> RenameCollectionResult:
        """写锁内重命名合集：先改 SQLite（name+image_path），后重命名目录。

        目录 rename 失败时调 ``rename_collection(cid, old_name)`` 回滚 SQLite
        与缓存；补偿失败记 critical 日志，抛 CollectionCreateError。

        Args:
            req: 写入任务单元，含源合集编号与已校验的新名称。

        Returns:
            重命名结果，含受 image_path 首段变更影响的条目数。

        Raises:
            CollectionNotFoundError: 源合集不存在。
            CollectionRenameTargetExistsError: 目标名称已登记。
            CollectionPathConflictError: 源/目标路径不是普通目录或目标已存在。
            CollectionCreateError: 目录 rename 失败且补偿失败。
        """
        collection = await asyncio.to_thread(
            self._metadata_store.get_collection, req.collection_id
        )
        if collection is None:
            raise CollectionNotFoundError(str(req.collection_id))
        old_name = collection.name
        new_name = req.new_collection_name

        existing = await asyncio.to_thread(
            self._metadata_store.get_collection_by_name, new_name
        )
        if existing is not None:
            raise CollectionRenameTargetExistsError(existing)

        old_dir = self._memes_dir / old_name
        await asyncio.to_thread(self._get_collection_directory_identity, old_dir)
        new_dir = self._memes_dir / new_name
        if new_dir.exists():
            raise CollectionPathConflictError(new_name)

        entry_count = await asyncio.to_thread(
            self._metadata_store.collection_entry_count, req.collection_id
        )

        await asyncio.to_thread(
            self._metadata_store.rename_collection,
            req.collection_id,
            new_name,
        )

        try:
            old_dir.rename(new_dir)
        except OSError as exc:
            # SQLite 已改、目录未动：回滚 SQLite 与缓存到旧名
            try:
                await asyncio.to_thread(
                    self._metadata_store.rename_collection,
                    req.collection_id,
                    old_name,
                )
            except Exception as rollback_exc:
                logger.critical(
                    "重命名目录失败且回滚 SQLite 失败: id=%d, old=%r, new=%r, error=%s",
                    req.collection_id,
                    old_name,
                    new_name,
                    rollback_exc,
                    exc_info=True,
                )
                raise CollectionCreateError(
                    "重命名合集失败且回滚失败"
                ) from rollback_exc
            logger.critical(
                "重命名目录失败，已回滚 SQLite: id=%d, old=%r, new=%r, error=%s",
                req.collection_id,
                old_name,
                new_name,
                exc,
                exc_info=True,
            )
            raise CollectionCreateError(
                "重命名合集目录失败"
            ) from exc

        logger.info(
            "合集重命名完成: id=%d, old=%r, new=%r, entries=%d",
            req.collection_id,
            old_name,
            new_name,
            entry_count,
        )
        return RenameCollectionResult(
            collection=MemeCollection(req.collection_id, new_name),
            old_name=old_name,
            new_name=new_name,
            entry_count=entry_count,
        )
```

- [ ] **Step 4: 注册到 `_write_dispatch` 字典**

在 `_write_dispatch` 字典里，Task 3 新增的 `WriteOp.DELETE_COLLECTION: self._execute_delete_collection_guarded,` 之后新增：

```python
            WriteOp.RENAME_COLLECTION: self._execute_rename_collection_guarded,
```

- [ ] **Step 5: 实现门面 `IndexManager.rename_collection`**

在 `bot/index_manager/manager.py` 的 `delete_collection` 方法之后（Task 3 新增代码后），新增。先确认顶部 import 已含 `RenameCollectionResult`、`validate_collection_name`（后者已在 manager.py 第 16 行 import）、`CollectionNotFoundError`（方法内局部 import）：

```python
    async def rename_collection(
        self, raw: str, new_name: str
    ) -> RenameCollectionResult:
        """重命名合集：改 DB name + 重命名目录 + 更新该合集 image_path 首段。

        collection_id 不变，chroma 与 ChatScope 不受影响。

        Args:
            raw: 源合集编号或精确名称（编号优先、名称兜底）。
            new_name: 新合集名称（走 validate_collection_name 校验，不能与已登记名重复）。

        Returns:
            重命名结果，含旧名、新名与受影响条目数。

        Raises:
            InvalidCollectionNameError: 新名称非法。
            CollectionNotFoundError: 源合集不存在。
            CollectionRenameTargetExistsError: 目标名称已登记。
            CollectionPathConflictError: 源/目标路径不是普通目录或目标已存在。
            CollectionCreateError: 目录 rename 失败且补偿失败。
            RefreshInProgressError: 索引刷新正在执行。
            IndexAddCancelledError: Bot 正在关闭或写入 worker 被取消。
        """
        if self._shutting_down:
            raise IndexAddCancelledError("Bot 正在关闭")
        if self._refresh_active:
            raise RefreshInProgressError("索引正在刷新，请稍后再试")

        from bot.engine.collection_manager import CollectionNotFoundError

        validated_new_name = validate_collection_name(new_name)
        collection = await asyncio.to_thread(
            self._collection_manager.resolve_collection, raw
        )
        self._ensure_write_worker()
        loop = asyncio.get_running_loop()
        future: asyncio.Future[RenameCollectionResult] = loop.create_future()
        req = _WriteRequest(
            op=WriteOp.RENAME_COLLECTION,
            future=future,  # type: ignore[arg-type]  # ty: ignore[invalid-argument-type]
            collection_id=collection.id,
            new_collection_name=validated_new_name,
        )
        return await self._coordinator.submit(req)
```

- [ ] **Step 6: 运行测试确认通过**

Run: `uv run pytest tests/unit/engine/test_index_manager_rename_collection.py -v`
Expected: 全部 PASS

- [ ] **Step 7: 类型检查与 lint**

Run: `uv run ty check bot/index_manager/write_coordinator.py bot/index_manager/manager.py` 和 `uv run ruff check bot/index_manager/write_coordinator.py bot/index_manager/manager.py tests/unit/engine/test_index_manager_rename_collection.py`
Expected: 无错误。

- [ ] **Step 8: 回归测试**

Run: `uv run pytest tests/unit/engine/test_index_manager_create_collection.py tests/unit/engine/test_index_manager_delete_collection.py tests/unit/engine/test_index_manager_move.py -v`
Expected: 全部 PASS。

- [ ] **Step 9: 提交（需用户审核）**

```bash
git add bot/index_manager/write_coordinator.py bot/index_manager/manager.py tests/unit/engine/test_index_manager_rename_collection.py
git commit -m "feat(index_manager): 新增 rename_collection 先改 SQLite 后重命名目录"
```

---

### Task 5: 插件层 collection.py 子命令派发

**Files:**
- Modify: `bot/plugins/collection.py`
- Test: `tests/unit/plugins/test_collection.py`（新增 delete/rename 用例 + 调整现有 `test_invalid_subcommand_replies_usage`）

**Interfaces:**
- Consumes（from Task 2/3/4）: `IndexManager.delete_collection`、`IndexManager.rename_collection`；`CollectionNotFoundError`、`CollectionNotEmptyError`、`CollectionDeleteError`、`CollectionRenameTargetExistsError`、`DeleteCollectionResult`、`RenameCollectionResult`（自 `bot.index_manager`）；现有 `InvalidCollectionNameError`、`CollectionAlreadyExistsError`、`CollectionPathConflictError`、`CollectionCreateError`、`RefreshInProgressError`、`IndexAddCancelledError`、`CreateCollectionResult`。
- Produces: `handle_collection` 按 `create`/`delete`/`rename` 子命令派发；三个子命令各自的错误映射与回复文案。

**实现要点**：
- 现有 `handle_collection` 只处理 `create`，用 `parts = text.split(maxsplit=1)` 取 `create` + 名称。改为按 `parts[0]` 子命令派发：`create`（2 段）、`delete`（2 段）、`rename`（3 段）。
- 现有 `test_collection.py` 的 `test_invalid_subcommand_replies_usage` 参数化含 `"delete 新三国"`——当前断言回复 create 用法。实现 delete 后此用例需调整：`"delete 新三国"` 不再是非法子命令。改为只保留 `""`、`"create"`（缺名称）、`"bogus 新三国"`（未知子命令）三类。
- 三个子命令共用授权、私聊、会话互斥、`get_index_manager` 检查、`finally deactivate_chat` 骨架。用三个内部 async 函数 `_handle_create`/`_handle_delete`/`_handle_rename` 分流，避免 `handle_collection` 膨胀。
- `delete`/`rename` 无确认等待态，与 `create` 一样直接调 `index_manager` 方法并回复。
- 错误映射严格按 spec §3.4 表。

- [ ] **Step 1: 调整现有占位测试**

读 `tests/unit/plugins/test_collection.py` 的 `test_invalid_subcommand_retries_usage`（约 123-137 行）。该测试参数化 `["", "create", "delete 新三国"]`，断言全部回复 `"用法：/collection create <名称>"`。实现 delete 子命令后 `"delete 新三国"` 变成有效 delete 调用。先调整该测试的参数化与断言（按子命令分别断言各自用法）：

```python
@pytest.mark.asyncio
@pytest.mark.parametrize(
    ("raw", "expected"),
    [
        ("", "用法：/collection create <名称>"),
        ("create", "用法：/collection create <名称>"),
        ("delete", "用法：/collection delete <编号|名称>"),
        ("rename", "用法：/collection rename <旧编号|名称> <新名称>"),
        ("rename 新三国", "用法：/collection rename <旧编号|名称> <新名称>"),
        ("bogus 新三国", "用法：/collection create <名称>"),
    ],
)
async def test_invalid_subcommand_replies_usage(raw: str, expected: str) -> None:
    matcher = _matcher()
    with (
        patch.object(collection, "is_authorized", return_value=True),
        patch.object(collection.session_manager, "activate_chat", return_value=True),
        patch.object(collection.session_manager, "deactivate_chat") as deactivate,
    ):
        await handle_collection(_bot(), _event(), matcher, _args(raw))

    assert extract_message_text(matcher.finish.await_args.args[0]) == expected
    deactivate.assert_called_once_with(_scope())
```

注意：`"bogus 新三国"`（未知子命令）按 spec §7.3 回复 create 用法（默认/未知子命令兜底）——在实现里确认未知子命令回 create 用法。

Run: `uv run pytest tests/unit/plugins/test_collection.py::test_invalid_subcommand_replies_usage -v`
Expected: FAIL（当前实现把 `delete`/`rename` 都当非法、统一回 create 用法；新断言要求 `delete` 回 delete 用法）。

- [ ] **Step 2: 重写 `collection.py` 按子命令派发**

读 `bot/plugins/collection.py` 全文（已在 spec 阶段读过，约 144 行）。用以下内容替换 `handle_collection` 及模块顶部常量/import。保留 `collection_cmd` 注册与 `_format_success`。

顶部 import 区新增（在现有 `from bot.index_manager import (...)` 块里补新符号）：

```python
from bot.index_manager import (
    CollectionAlreadyExistsError,
    CollectionCreateError,
    CollectionDeleteError,
    CollectionNotFoundError,  # 新增（自 bot.index_manager re-export? 见下注）
    CollectionNotEmptyError,
    CollectionPathConflictError,
    CollectionRenameTargetExistsError,
    CreateCollectionResult,
    DeleteCollectionResult,
    IndexAddCancelledError,
    RefreshInProgressError,
    RenameCollectionResult,
)
```

注：`CollectionNotFoundError` 定义在 `bot.engine.collection_manager`，未在 `bot.index_manager` re-export。保持与 `_collection_utils.py` 一致从 `bot.engine.collection_manager` import：在现有 `from bot.engine.collection_manager import InvalidCollectionNameError` 行扩展为：

```python
from bot.engine.collection_manager import (
    CollectionNotFoundError,
    InvalidCollectionNameError,
)
```

模块常量区（`_USAGE` 旁）新增：

```python
_USAGE_CREATE = "用法：/collection create <名称>"
_USAGE_DELETE = "用法：/collection delete <编号|名称>"
_USAGE_RENAME = "用法：/collection rename <旧编号|名称> <新名称>"
_USAGE = _USAGE_CREATE  # 未知子命令兜底用 create 用法
_INVALID_NAME = (
    "合集名称无效：不能为空、不能包含空白或路径字符，也不能使用保留名称"
)
```

（保留原 `_INVALID_NAME`；`_USAGE` 仍作为未知子命令兜底，指向 `_USAGE_CREATE`。）

替换 `handle_collection` 为派发骨架 + 三个内部函数。完整替换从 `@collection_cmd.handle()` 到文件末尾：

```python
def _format_create_success(result: CreateCollectionResult) -> str:
    """格式化合集创建成功回复。

    Args:
        result: 已持久化的合集创建结果。

    Returns:
        用户可见的成功文本。
    """
    collection = result.collection
    lines = [
        "合集创建完成 ✅",
        f"编号：{collection.id}",
        f"名称：{collection.name}",
    ]
    if result.registered_existing_directory:
        lines.append("已登记现有目录；目录中的图片请执行 /refresh 建立索引")
    return "\n".join(lines)


def _format_delete_success(result: DeleteCollectionResult) -> str:
    """格式化合集删除成功回复。

    Args:
        result: 已持久化的合集删除结果。

    Returns:
        用户可见的成功文本。
    """
    collection = result.collection
    lines = [
        "合集已删除 ✅",
        f"编号：{collection.id}",
        f"名称：{collection.name}",
    ]
    if result.reset_scope_count > 0:
        lines.append(
            f"已把 {result.reset_scope_count} 个聊天窗口的合集选择回退到全部合集"
        )
    return "\n".join(lines)


def _format_rename_success(result: RenameCollectionResult) -> str:
    """格式化合集重命名成功回复。

    Args:
        result: 已持久化的合集重命名结果。

    Returns:
        用户可见的成功文本。
    """
    collection = result.collection
    return "\n".join(
        [
            "合集已重命名 ✅",
            f"编号：{collection.id}",
            f"旧名称：{result.old_name}",
            f"新名称：{result.new_name}",
            f"更新条目：{result.entry_count}",
        ]
    )


@collection_cmd.handle()
async def handle_collection(
    bot: Bot,
    event: MessageEvent,
    matcher: Matcher,
    args: Message = CommandArg(),
) -> None:
    """处理合集 create/delete/rename 子命令。

    Args:
        bot: OneBot V11 Bot 实例。
        event: 私聊或群聊消息事件。
        matcher: NoneBot2 Matcher 实例。
        args: 命令后的完整参数消息。
    """
    _ = bot
    user_id = event.get_user_id()
    scope = ChatScope.from_event(event)
    request_id = generate_request_id()
    with set_request_id(request_id):
        if not is_authorized(user_id):
            log_unauthorized(user_id, "collection")
            await matcher.finish(None)
            return

        if event.message_type != "private":
            await reply_utils.finish(event, matcher, "此命令仅限私聊使用")
            return

        if not session_manager.activate_chat(scope, "collection", matcher):
            await reply_utils.finish(
                event, matcher, "已有命令在处理中，请先 /cancel"
            )
            return

        try:
            text = args.extract_plain_text().strip()
            parts = text.split()
            if not parts:
                await reply_utils.finish(event, matcher, _USAGE)
                return
            subcommand = parts[0]
            if subcommand == "create":
                await _handle_create(event, matcher, text)
            elif subcommand == "delete":
                await _handle_delete(event, matcher, text)
            elif subcommand == "rename":
                await _handle_rename(event, matcher, text)
            else:
                await reply_utils.finish(event, matcher, _USAGE)
        finally:
            session_manager.deactivate_chat(scope)


async def _get_manager_or_finish(
    event: MessageEvent, matcher: Matcher
) -> "object | None":
    """获取 IndexManager，未就绪时回复提示并返回 None。

    Args:
        event: 私聊消息事件。
        matcher: NoneBot2 Matcher 实例。

    Returns:
        IndexManager 实例；未就绪时返回 None（已向用户回复）。
    """
    try:
        return get_index_manager()
    except RuntimeError:
        logger.error("IndexManager 尚未初始化")
        await reply_utils.finish(event, matcher, "服务未就绪，请稍后再试")
        return None


async def _handle_create(
    event: MessageEvent, matcher: Matcher, text: str
) -> None:
    """处理 create 子命令。

    Args:
        event: 私聊消息事件。
        matcher: NoneBot2 Matcher 实例。
        text: 已 strip 的命令参数原文。
    """
    parts = text.split(maxsplit=1)
    if len(parts) != 2 or parts[0] != "create":
        await reply_utils.finish(event, matcher, _USAGE_CREATE)
        return
    manager = await _get_manager_or_finish(event, matcher)
    if manager is None:
        return
    try:
        result = await manager.create_collection(parts[1])
    except InvalidCollectionNameError:
        await reply_utils.finish(event, matcher, _INVALID_NAME)
        return
    except CollectionAlreadyExistsError as exc:
        existing = exc.collection
        await reply_utils.finish(
            event,
            matcher,
            f"表情包合集已存在：{existing.name}（{existing.id}）",
        )
        return
    except CollectionPathConflictError:
        await reply_utils.finish(
            event, matcher, "无法创建合集：同名路径不是可用目录"
        )
        return
    except RefreshInProgressError:
        await reply_utils.finish(event, matcher, "索引正在刷新，请稍后再试")
        return
    except IndexAddCancelledError:
        await reply_utils.finish(event, matcher, "服务正在关闭，请稍后再试")
        return
    except CollectionCreateError:
        logger.exception("合集创建和目录补偿失败")
        await reply_utils.finish(event, matcher, "合集创建失败，请检查日志后重试")
        return
    except Exception:
        logger.exception("合集创建失败")
        await reply_utils.finish(event, matcher, "合集创建失败，请检查日志后重试")
        return
    await reply_utils.finish(event, matcher, _format_create_success(result))


async def _handle_delete(
    event: MessageEvent, matcher: Matcher, text: str
) -> None:
    """处理 delete 子命令。

    Args:
        event: 私聊消息事件。
        matcher: NoneBot2 Matcher 实例。
        text: 已 strip 的命令参数原文。
    """
    parts = text.split(maxsplit=1)
    if len(parts) != 2 or parts[0] != "delete":
        await reply_utils.finish(event, matcher, _USAGE_DELETE)
        return
    manager = await _get_manager_or_finish(event, matcher)
    if manager is None:
        return
    target = parts[1]
    try:
        result = await manager.delete_collection(target)
    except CollectionNotFoundError:
        await reply_utils.finish(
            event,
            matcher,
            f"未找到表情包合集：{target}\n发送 /switch 查看可用合集",
        )
        return
    except CollectionNotEmptyError:
        await reply_utils.finish(
            event, matcher, "合集不为空，请先 /move 或 /del 清空后再删除"
        )
        return
    except CollectionPathConflictError:
        await reply_utils.finish(
            event, matcher, "无法删除合集：同名路径不是可用目录"
        )
        return
    except RefreshInProgressError:
        await reply_utils.finish(event, matcher, "索引正在刷新，请稍后再试")
        return
    except IndexAddCancelledError:
        await reply_utils.finish(event, matcher, "服务正在关闭，请稍后再试")
        return
    except CollectionDeleteError:
        logger.exception("合集删除和目录补偿失败")
        await reply_utils.finish(event, matcher, "合集删除失败，请检查日志后重试")
        return
    except Exception:
        logger.exception("合集删除失败")
        await reply_utils.finish(event, matcher, "合集删除失败，请检查日志后重试")
        return
    await reply_utils.finish(event, matcher, _format_delete_success(result))


async def _handle_rename(
    event: MessageEvent, matcher: Matcher, text: str
) -> None:
    """处理 rename 子命令。

    Args:
        event: 私聊消息事件。
        matcher: NoneBot2 Matcher 实例。
        text: 已 strip 的命令参数原文。
    """
    parts = text.split()
    if len(parts) != 3 or parts[0] != "rename":
        await reply_utils.finish(event, matcher, _USAGE_RENAME)
        return
    _, source_raw, new_name = parts
    manager = await _get_manager_or_finish(event, matcher)
    if manager is None:
        return
    try:
        result = await manager.rename_collection(source_raw, new_name)
    except InvalidCollectionNameError:
        await reply_utils.finish(event, matcher, _INVALID_NAME)
        return
    except CollectionNotFoundError:
        await reply_utils.finish(
            event,
            matcher,
            f"未找到表情包合集：{source_raw}\n发送 /switch 查看可用合集",
        )
        return
    except CollectionRenameTargetExistsError as exc:
        existing = exc.collection
        await reply_utils.finish(
            event,
            matcher,
            f"合集名称已存在：{existing.name}（{existing.id}）",
        )
        return
    except CollectionPathConflictError:
        await reply_utils.finish(
            event, matcher, "无法重命名：目标名称对应路径不是可用目录"
        )
        return
    except RefreshInProgressError:
        await reply_utils.finish(event, matcher, "索引正在刷新，请稍后再试")
        return
    except IndexAddCancelledError:
        await reply_utils.finish(event, matcher, "服务正在关闭，请稍后再试")
        return
    except CollectionCreateError:
        logger.exception("合集重命名和目录补偿失败")
        await reply_utils.finish(event, matcher, "合集重命名失败，请检查日志后重试")
        return
    except Exception:
        logger.exception("合集重命名失败")
        await reply_utils.finish(event, matcher, "合集重命名失败，请检查日志后重试")
        return
    await reply_utils.finish(event, matcher, _format_rename_success(result))
```

注：`_get_manager_or_finish` 返回类型标注 `"object | None"` 是为避免在插件层 import `IndexManager`（保持插件层不依赖门面具体类，与现有 `collection.py` 用 `get_index_manager()` 返回值鸭子类型一致）。若 ruff/ty 报unused import 或类型告警，调整注解。

- [ ] **Step 3: 调整现有 create 测试的 `_format_success` 引用**

现有 `test_collection.py` 的 `test_create_success_keeps_current_selection` 断言回复 `"合集创建完成 ✅\n编号：3\n名称：新三国"`——新实现 `_format_create_success` 产出相同文本，无需改测试。但现有测试 mock `manager.create_collection = AsyncMock(return_value=_result())`，`manager` 是 `MagicMock()`，`_get_manager_or_finish` 调 `get_index_manager()` 返回该 mock，`manager.create_collection(...)` 仍被 await——逻辑等价。**无需改现有 create 用例**，只跑一遍确认。

Run: `uv run pytest tests/unit/plugins/test_collection.py -v -k "create or invalid_subcommand or unauthorized or group or active_session"`
Expected: 全部 PASS（含调整后的 `test_invalid_subcommand_replies_usage`）。

- [ ] **Step 4: 新增 delete 插件用例**

在 `tests/unit/plugins/test_collection.py` 末尾新增（复用现有 `_event`/`_args`/`_matcher`/`_bot`/`_scope` helper 与 `patch` 模式）：

```python
def _delete_result(*, reset_scopes: int = 0) -> DeleteCollectionResult:
    return DeleteCollectionResult(
        collection=MemeCollection(3, "新三国"),
        reset_scope_count=reset_scopes,
    )


@pytest.mark.asyncio
async def test_delete_success_replies_summary() -> None:
    manager = MagicMock()
    manager.delete_collection = AsyncMock(return_value=_delete_result(reset_scopes=2))
    matcher = _matcher()
    with (
        patch.object(collection, "is_authorized", return_value=True),
        patch.object(collection.session_manager, "activate_chat", return_value=True),
        patch.object(collection.session_manager, "deactivate_chat") as deactivate,
        patch.object(collection, "get_index_manager", return_value=manager),
    ):
        await handle_collection(_bot(), _event(), matcher, _args("delete 新三国"))

    manager.delete_collection.assert_awaited_once_with("新三国")
    text = extract_message_text(matcher.finish.await_args.args[0])
    assert text == (
        "合集已删除 ✅\n编号：3\n名称：新三国\n"
        "已把 2 个聊天窗口的合集选择回退到全部合集"
    )
    deactivate.assert_called_once_with(_scope())


@pytest.mark.asyncio
async def test_delete_success_without_scope_reset_omits_line() -> None:
    manager = MagicMock()
    manager.delete_collection = AsyncMock(return_value=_delete_result(reset_scopes=0))
    matcher = _matcher()
    with (
        patch.object(collection, "is_authorized", return_value=True),
        patch.object(collection.session_manager, "activate_chat", return_value=True),
        patch.object(collection.session_manager, "deactivate_chat"),
        patch.object(collection, "get_index_manager", return_value=manager),
    ):
        await handle_collection(_bot(), _event(), matcher, _args("delete 3"))

    manager.delete_collection.assert_awaited_once_with("3")
    text = extract_message_text(matcher.finish.await_args.args[0])
    assert text == "合集已删除 ✅\n编号：3\n名称：新三国"


@pytest.mark.asyncio
@pytest.mark.parametrize(
    ("error", "target", "expected"),
    [
        (
            CollectionNotFoundError("新三国"),
            "新三国",
            "未找到表情包合集：新三国\n发送 /switch 查看可用合集",
        ),
        (
            CollectionNotEmptyError("新三国"),
            "新三国",
            "合集不为空，请先 /move 或 /del 清空后再删除",
        ),
        (
            CollectionPathConflictError("新三国"),
            "新三国",
            "无法删除合集：同名路径不是可用目录",
        ),
        (RefreshInProgressError("raw"), "新三国", "索引正在刷新，请稍后再试"),
        (IndexAddCancelledError("Bot 正在关闭"), "新三国", "服务正在关闭，请稍后再试"),
        (CollectionDeleteError("internal"), "新三国", "合集删除失败，请检查日志后重试"),
    ],
)
async def test_delete_errors_have_fixed_messages(
    error: Exception, target: str, expected: str
) -> None:
    manager = MagicMock()
    manager.delete_collection = AsyncMock(side_effect=error)
    matcher = _matcher()
    with (
        patch.object(collection, "is_authorized", return_value=True),
        patch.object(collection.session_manager, "activate_chat", return_value=True),
        patch.object(collection.session_manager, "deactivate_chat"),
        patch.object(collection, "get_index_manager", return_value=manager),
    ):
        await handle_collection(_bot(), _event(), matcher, _args(f"delete {target}"))

    assert extract_message_text(matcher.finish.await_args.args[0]) == expected
    assert "internal" not in expected
```

新增 import（文件顶部 `from bot.index_manager import (...)` 块补 `CollectionDeleteError`、`CollectionNotFoundError`、`CollectionNotEmptyError`、`DeleteCollectionResult`；`CollectionNotFoundError` 实际来自 `bot.engine.collection_manager`，但插件测试现有 import 从 `bot.index_manager` 取——确认 `CollectionNotFoundError` 是否在 `bot.index_manager.__init__` re-export。**若未 re-export，从 `bot.engine.collection_manager` import**）。

- [ ] **Step 5: 运行 delete 插件测试**

Run: `uv run pytest tests/unit/plugins/test_collection.py -v -k delete`
Expected: 全部 PASS

- [ ] **Step 6: 新增 rename 插件用例**

在 `tests/unit/plugins/test_collection.py` 继续新增：

```python
def _rename_result() -> RenameCollectionResult:
    return RenameCollectionResult(
        collection=MemeCollection(3, "旧三国"),
        old_name="新三国",
        new_name="旧三国",
        entry_count=12,
    )


@pytest.mark.asyncio
async def test_rename_success_replies_summary() -> None:
    manager = MagicMock()
    manager.rename_collection = AsyncMock(return_value=_rename_result())
    matcher = _matcher()
    with (
        patch.object(collection, "is_authorized", return_value=True),
        patch.object(collection.session_manager, "activate_chat", return_value=True),
        patch.object(collection.session_manager, "deactivate_chat") as deactivate,
        patch.object(collection, "get_index_manager", return_value=manager),
    ):
        await handle_collection(
            _bot(), _event(), matcher, _args("rename 新三国 旧三国")
        )

    manager.rename_collection.assert_awaited_once_with("新三国", "旧三国")
    text = extract_message_text(matcher.finish.await_args.args[0])
    assert text == (
        "合集已重命名 ✅\n编号：3\n旧名称：新三国\n新名称：旧三国\n更新条目：12"
    )
    deactivate.assert_called_once_with(_scope())


@pytest.mark.asyncio
@pytest.mark.parametrize(
    ("error", "expected"),
    [
        (
            InvalidCollectionNameError("坏 名称"),
            "合集名称无效：不能为空、不能包含空白或路径字符，也不能使用保留名称",
        ),
        (
            CollectionNotFoundError("新三国"),
            "未找到表情包合集：新三国\n发送 /switch 查看可用合集",
        ),
        (
            CollectionRenameTargetExistsError(MemeCollection(2, "甄嬛传")),
            "合集名称已存在：甄嬛传（2）",
        ),
        (
            CollectionPathConflictError("旧三国"),
            "无法重命名：目标名称对应路径不是可用目录",
        ),
        (RefreshInProgressError("raw"), "索引正在刷新，请稍后再试"),
        (IndexAddCancelledError("Bot 正在关闭"), "服务正在关闭，请稍后再试"),
        (CollectionCreateError("internal"), "合集重命名失败，请检查日志后重试"),
    ],
)
async def test_rename_errors_have_fixed_messages(error: Exception, expected: str) -> None:
    manager = MagicMock()
    manager.rename_collection = AsyncMock(side_effect=error)
    matcher = _matcher()
    with (
        patch.object(collection, "is_authorized", return_value=True),
        patch.object(collection.session_manager, "activate_chat", return_value=True),
        patch.object(collection.session_manager, "deactivate_chat"),
        patch.object(collection, "get_index_manager", return_value=manager),
    ):
        await handle_collection(
            _bot(), _event(), matcher, _args("rename 新三国 旧三国")
        )

    assert extract_message_text(matcher.finish.await_args.args[0]) == expected
    assert "internal" not in expected
```

新增 import：`CollectionRenameTargetExistsError`、`RenameCollectionResult` 从 `bot.index_manager`。

- [ ] **Step 7: 运行全部 collection 插件测试**

Run: `uv run pytest tests/unit/plugins/test_collection.py -v`
Expected: 全部 PASS（create + delete + rename + 用法 + 授权 + 群聊 + 会话互斥）。

- [ ] **Step 8: 类型检查与 lint**

Run: `uv run ty check bot/plugins/collection.py` 和 `uv run ruff check bot/plugins/collection.py tests/unit/plugins/test_collection.py`
Expected: 无错误。

- [ ] **Step 9: 回归测试**

Run: `uv run pytest tests/unit/plugins/ -v`
Expected: 全部 PASS（确认改动未影响其他插件）。

- [ ] **Step 10: 提交（需用户审核）**

```bash
git add bot/plugins/collection.py tests/unit/plugins/test_collection.py
git commit -m "feat(plugins): /collection 新增 delete 与 rename 子命令派发"
```

---

### Task 6: 帮助文本与文档同步

**Files:**
- Modify: `bot/plugins/_help_text.py`
- Test: `tests/unit/plugins/test_help.py`（确认/新增断言）
- Modify: `docs/PRD.md`
- Modify: `CONTEXT.md`
- Modify: `README.md`（若有命令清单）

**Interfaces:**
- Consumes: 无（纯文本/文档）。
- Produces: `HELP_TEXT` 含三行 collection 子命令；PRD/CONTEXT/README 文档同步。

- [ ] **Step 1: 更新 `HELP_TEXT`**

在 `bot/plugins/_help_text.py` 的 `HELP_TEXT` 常量里，把现有单行：

```
/collection create <名称>：创建表情包合集
```

替换为三行：

```
/collection create <名称>：创建表情包合集
/collection delete <编号|名称>：删除空合集
/collection rename <旧编号|名称> <新名称>：重命名合集
```

`HELP_TEXT_GROUP`（群聊版）不动（组 A 命令不在群聊帮助展示）。

- [ ] **Step 2: 检查/更新 help 测试**

读 `tests/unit/plugins/test_help.py`，确认它是否断言 `HELP_TEXT` 或 `help_text_for("private")` 的具体内容。若有对 collection 行的断言，更新为三行；若无具体行断言，新增一个断言 private 帮助含三行：

```python
def test_private_help_contains_collection_subcommands() -> None:
    text = help_text_for("private")
    assert "/collection create <名称>：创建表情包合集" in text
    assert "/collection delete <编号|名称>：删除空合集" in text
    assert "/collection rename <旧编号|名称> <新名称>：重命名合集" in text


def test_group_help_excludes_collection_delete_rename() -> None:
    text = help_text_for("group")
    assert "/collection delete" not in text
    assert "/collection rename" not in text
    # 群聊帮助本就不展示任何 /collection 行（组 A 仅私聊）
    assert "/collection" not in text
```

（根据现有测试的实际 import 与函数名调整；若现有测试已覆盖 `help_text_for`，沿用其风格。）

Run: `uv run pytest tests/unit/plugins/test_help.py -v`
Expected: PASS。

- [ ] **Step 3: 更新 `docs/PRD.md`**

按 spec §8.2 更新：
1. **3.3 节**：把"功能三：创建合集"标题改为"功能三：合集管理"，扩成 create / delete / rename 三子节。补 delete/rename 的触发方式、流程、名称规则、一致性与补偿、交互约束。
2. **3.5 帮助命令清单**：把 `/collection create <名称>：创建表情包合集` 一行扩为三行。
3. **3.6 组 A 命令分组表**与 **3.6 并发约束**：补 `/collection delete`、`/collection rename`（与 create 同属 Write Worker 串行 + 全局写锁）。
4. **5 边界情况表**：补行——
   - `/collection delete` 非空合集 → "合集不为空，请先 /move 或 /del 清空后再删除"
   - `/collection delete` 不存在 → "未找到表情包合集：{目标}\n发送 /switch 查看可用合集"
   - `/collection delete` rmdir 失败 → DB 未动，"合集删除失败，请检查日志后重试"
   - `/collection rename` 目标名已登记 → "合集名称已存在：{name}（{id}）"
   - `/collection rename` 目标路径已存在 → "无法重命名：目标名称对应路径不是可用目录"
   - `/collection rename` 目录 rename 失败 → 回滚 SQLite，"合集重命名失败，请检查日志后重试"
   - `/collection delete`/`rename` 群聊 @bot → "此命令仅限私聊使用"
   - `/collection delete`/`rename` 刷新中 → "索引正在刷新，请稍后再试"

文案与 spec §7、§3.4 严格一致。

- [ ] **Step 4: 更新 `CONTEXT.md`**

按 spec §8.3：
1. 术语表 `/collection create` 条目扩成三条（`/collection create`、`/collection delete`、`/collection rename`），各写触发方式、权限、直接执行无确认、底层行为。
2. "创建合集"术语旁补"删除合集""重命名合集"两条：
   - **删除合集**：`/collection delete <编号|名称>` 的行为；授权用户仅限私聊直接执行；通过 IndexManager Write Worker 在写锁内先 rmdir 空目录、后删 `meme_collection` 记录并回退引用它的所有 ChatScope 到 0；非空合集拒绝。
   - **重命名合集**：`/collection rename <旧编号|名称> <新名称>` 的行为；授权用户仅限私聊直接执行；通过 IndexManager Write Worker 在写锁内先改 `meme_collection.name` 与该合集所有 `meme.image_path` 首段、后重命名 `memes/` 目录；`collection_id` 不变，chroma 与 ChatScope 不受影响；目录 rename 失败回滚 SQLite。

- [ ] **Step 5: 检查 `README.md`**

读 `README.md`，若有命令清单（类似 `/collection create`）则同步三行；若无命令清单则不动。用 `rg -n "collection" README.md` 确认。

- [ ] **Step 6: lint 检查**

Run: `uv run ruff check bot/plugins/_help_text.py tests/unit/plugins/test_help.py`
Expected: 无错误。

- [ ] **Step 7: 提交（需用户审核）**

```bash
git add bot/plugins/_help_text.py tests/unit/plugins/test_help.py docs/PRD.md CONTEXT.md README.md
git commit -m "docs: 同步合集 delete/rename 命令到帮助、PRD、CONTEXT 与 README"
```

---

### Task 7: 全量回归与收尾

**Files:** 无新改动，仅验证。

- [ ] **Step 1: 全量类型检查**

Run: `uv run ty check`
Expected: 全仓 ty 清零。

- [ ] **Step 2: 全量 lint**

Run: `uv run ruff check`
Expected: 无告警。

- [ ] **Step 3: 全量测试**

Run: `uv run pytest -v`
Expected: 全部 PASS。重点关注：
- `tests/unit/engine/test_metadata_store.py`（rename_collection）
- `tests/unit/engine/test_index_manager_delete_collection.py`
- `tests/unit/engine/test_index_manager_rename_collection.py`
- `tests/unit/engine/test_index_manager_create_collection.py`（未破坏）
- `tests/unit/engine/test_index_manager_delete.py`（meme delete 未破坏）
- `tests/unit/engine/test_index_manager_move.py`（move 未破坏）
- `tests/unit/plugins/test_collection.py`
- `tests/unit/plugins/test_help.py`
- `tests/unit/plugins/test_plain_text.py`（未知命令帮助摘要自动同步）

- [ ] **Step 4: 一致性核对**

人工核对（或用 `rg` 搜索确认）：
- `WriteOp` 枚举有 `DELETE_COLLECTION`、`RENAME_COLLECTION`：`rg "DELETE_COLLECTION|RENAME_COLLECTION" bot/index_manager/`
- `_write_dispatch` 字典含两个新执行器：`rg "_execute_delete_collection_guarded|_execute_rename_collection_guarded" bot/index_manager/write_coordinator.py`
- `_WriteRequest` 有 `collection_id`、`new_collection_name` 字段：`rg "collection_id: int = 0|new_collection_name: str = " bot/index_manager/index_types.py`
- `__init__.py` re-export 5 个新符号：`rg "CollectionDeleteError|CollectionNotEmptyError|CollectionRenameTargetExistsError|DeleteCollectionResult|RenameCollectionResult" bot/index_manager/__init__.py`
- `MetadataStore.rename_collection` 存在：`rg "def rename_collection" bot/engine/metadata_store.py`
- `IndexManager.delete_collection`/`rename_collection` 存在：`rg "async def (delete_collection|rename_collection)" bot/index_manager/manager.py`

- [ ] **Step 5: 更新 memory**

若用户确认，更新 `MEMORY.md` 与对应 memory 文件，记录"合集 delete/rename 命令已实现"的项目记忆（type=project）。仅在用户审核通过、实现合并后进行。

- [ ] **Step 6: 最终提交（需用户审核）**

若前面任务已在 feature 分支提交，此处可整理 commit 或提 PR；若用户要求在 main 合并，按项目规则由用户审核后合并。本步骤不自行合并。

---

## Self-Review（计划自审）

**1. Spec 覆盖**：逐条核对 spec 各节 → 任务映射：
- §3 命令接口与插件层 → Task 5 ✓
- §4 MetadataStore 层 → Task 1 ✓
- §5.1-5.3 WriteOp/请求字段/dispatch → Task 2 + Task 3/4 ✓
- §5.4 delete 执行器（先 rmdir 后 DB）→ Task 3 ✓
- §5.5 rename 执行器（先 SQLite 后文件 + 补偿）→ Task 4 ✓
- §5.6 门面薄入口 → Task 3/4 ✓
- §5.7 异常归属（rename 补偿复用 CollectionCreateError）→ Task 4 ✓
- §6 异常/结果类型 → Task 2 ✓
- §7 回复文案 → Task 5 ✓
- §8.1 帮助文本 → Task 6 ✓
- §8.2 PRD → Task 6 ✓
- §8.3 CONTEXT → Task 6 ✓
- §8.4 README → Task 6 ✓
- §9 测试 → Task 1/3/4/5/6 + Task 7 回归 ✓
- §10 改动文件清单 → File Structure 表 ✓（已据实修正测试路径：无 `tests/unit/engine/plugins/`，engine 测试在 `tests/unit/engine/`，插件测试在 `tests/unit/plugins/`）

**2. 占位符扫描**：无 TBD/TODO/"implement later"/"add error handling"等。每个代码步骤含完整代码。✓

**3. 类型一致性**：
- `rename_collection(collection_id, new_name) -> MemeCollection`：Task 1 定义，Task 4 调用签名一致 ✓
- `delete_collection(raw) -> DeleteCollectionResult`：Task 3 定义，Task 5 调用 `manager.delete_collection(target)` 一致 ✓
- `rename_collection(raw, new_name) -> RenameCollectionResult`：Task 4 定义，Task 5 调用 `manager.rename_collection(source_raw, new_name)` 一致 ✓
- `DeleteCollectionResult(collection, reset_scope_count)`、`RenameCollectionResult(collection, old_name, new_name, entry_count)`：Task 2 定义，Task 3/4 构造、Task 5 测试断言字段名一致 ✓
- `CollectionNotEmptyError`、`CollectionDeleteError`、`CollectionRenameTargetExistsError`：Task 2 定义，Task 3/4/5 引用一致 ✓
- `_WriteRequest.collection_id`、`new_collection_name`：Task 2 定义，Task 3/4 构造一致 ✓
- `WriteOp.DELETE_COLLECTION`/`RENAME_COLLECTION`：Task 2 定义，Task 3/4 构造一致 ✓

**4. 潜在风险点（已在对应任务标注）**：
- Task 1：`MemeEntry` frozen，必须先改 `_id_to_collections` name 再 `dataclasses.replace`（实现要点已点明）。
- Task 5：`CollectionNotFoundError` 的 import 来源（`bot.engine.collection_manager` 而非 `bot.index_manager`，因未 re-export）——Task 5 Step 2 已注明。若 Task 2 顺手在 `__init__.py` re-export 了它则可从 `bot.index_manager` 取；本计划选择不 re-export `CollectionNotFoundError`（它本属 collection_manager 域），与现有 `_collection_utils.py` 一致。
- Task 5：`_get_manager_or_finish` 返回类型 `"object | None"` 避免插件层 import 门面类——若 ty 报告调用 `manager.create_collection` 等属性不存在，可改为 `from bot.index_manager import IndexManager` 并标注 `IndexManager | None`（现有 `collection.py` 未 import IndexManager，但 ty 对 MagicMock 调用通常宽松；实现时按实际告警调整）。
- Task 5 Step 1：`"bogus 新三国"` 未知子命令回 create 用法——实现 Step 2 的 `else` 分支回 `_USAGE`（=`_USAGE_CREATE`），一致 ✓。
