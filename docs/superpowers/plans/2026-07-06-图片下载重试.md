# 图片下载重试机制实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `/add` 命令的图片下载增加基于 `api_retry` 的重试机制，覆盖网络/超时异常和 HTTP 5xx 错误，4xx 直接失败。

**Architecture:** 在 `bot/plugins/meme_add.py` 的 `_download_image` 上直接应用 `api_retry(extra_exceptions=(DownloadServerError,))`；模块内新增 `DownloadServerError` 用于标识可重试的 HTTP 5xx；函数内部先判断 `status_code >= 500` 抛出该异常，再调用 `raise_for_status()` 让 4xx 立即失败。

**Tech Stack:** Python 3.12, httpx, tenacity（通过项目 `api_retry`）, pytest, unittest.mock

---

## 文件变更清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `bot/plugins/meme_add.py` | 修改 | 导入 `api_retry`；新增 `DownloadServerError`；为 `_download_image` 添加重试装饰器和 5xx 检测 |
| `tests/unit/plugins/test_meme_add.py` | 修改 | 新增 `_download_image` 重试相关单元测试 |

---

## Task 1: 导入 `api_retry` 并新增 `DownloadServerError`

**Files:**
- Modify: `bot/plugins/meme_add.py`

- [ ] **Step 1: 在 `meme_add.py` 顶部导入 `api_retry`**

找到现有导入区域（约第 1–20 行），新增：

```python
from bot.engine.retry_config import api_retry
```

- [ ] **Step 2: 在模块内新增 `DownloadServerError` 异常**

在 `_download_image` 函数附近、其他辅助函数之前添加：

```python
class DownloadServerError(RuntimeError):
    """QQ 图片服务器返回 5xx，允许重试。"""
```

建议位置：`_download_image` 上方，与 `_get_extension` 等辅助函数同级。

- [ ] **Step 3: 语法检查**

Run: `uv run python -m compileall bot/plugins/meme_add.py`  
Expected: `Compiling 'bot/plugins/meme_add.py'...` 无错误

- [ ] **Step 4: Commit**

```bash
git add bot/plugins/meme_add.py
# 注意：按项目规定，提交需经用户审核，此处仅暂存
```

---

## Task 2: 为 `_download_image` 添加重试装饰器和 5xx 检测

**Files:**
- Modify: `bot/plugins/meme_add.py:263-281`

- [ ] **Step 1: 修改 `_download_image` 函数**

将现有实现：

```python
async def _download_image(url: str) -> tuple[bytes, httpx.Response]:
    """下载图片。

    Args:
        url: 图片 URL。

    Returns:
        (图片数据, HTTP 响应) 元组。

    Raises:
        httpx.HTTPError: 下载失败。
    """
    async with httpx.AsyncClient() as client:
        response = await client.get(
            url, timeout=DOWNLOAD_TIMEOUT, follow_redirects=True
        )
        response.raise_for_status()
        return response.content, response
```

替换为：

```python
@api_retry(extra_exceptions=(DownloadServerError,))
async def _download_image(url: str) -> tuple[bytes, httpx.Response]:
    """下载图片，支持网络/超时/5xx 重试。

    Args:
        url: 图片 URL。

    Returns:
        (图片数据, HTTP 响应) 元组。

    Raises:
        httpx.HTTPError: 4xx 客户端错误等不可重试错误。
        DownloadServerError: 5xx 服务器错误，由 api_retry 重试。
    """
    async with httpx.AsyncClient() as client:
        response = await client.get(
            url, timeout=DOWNLOAD_TIMEOUT, follow_redirects=True
        )
        if response.status_code >= 500:
            raise DownloadServerError(
                f"图片服务器错误 {response.status_code}: {url}"
            )
        response.raise_for_status()
        return response.content, response
```

- [ ] **Step 2: 运行现有 `/add` 插件单元测试，确保无回归**

Run: `uv run pytest tests/unit/plugins/test_meme_add.py -v`  
Expected: 全部通过（约 25 个测试）

- [ ] **Step 3: Commit**

```bash
git add bot/plugins/meme_add.py
# 提交需经用户审核
```

---

## Task 3: 新增 `_download_image` 重试单元测试

**Files:**
- Modify: `tests/unit/plugins/test_meme_add.py`

- [ ] **Step 1: 在测试文件中导入 AsyncMock（如尚未导入）**

确认测试文件顶部已有：

```python
from unittest.mock import AsyncMock, MagicMock, patch
```

- [ ] **Step 2: 新增 `_download_image` 测试类**

在 `TestFormatOcrText` 类之后、`TestHandleAdd` 类之前插入：

```python
class TestDownloadImageRetry:
    """_download_image 重试行为测试。"""

    @pytest.mark.asyncio
    async def test_5xx_retries_and_succeeds(self) -> None:
        """5xx 服务器错误应重试，最终成功时返回内容。"""
        bad_response = MagicMock()
        bad_response.status_code = 503
        bad_response.headers = {}

        good_response = MagicMock()
        good_response.status_code = 200
        good_response.headers = {"content-type": "image/jpeg"}
        good_response.content = b"fake_image"

        mock_client = MagicMock()
        mock_client.__aenter__ = AsyncMock(return_value=mock_client)
        mock_client.__aexit__ = AsyncMock(return_value=None)
        mock_client.get = AsyncMock(side_effect=[bad_response, good_response])

        with patch("httpx.AsyncClient", return_value=mock_client):
            content, response = await meme_add._download_image(
                "https://img.example.com/a.jpg"
            )

        assert content == b"fake_image"
        assert response is good_response
        assert mock_client.get.await_count == 2

    @pytest.mark.asyncio
    async def test_5xx_exhausts_retries_and_fails(self) -> None:
        """5xx 持续返回时，重试耗尽后抛出 DownloadServerError。"""
        bad_response = MagicMock()
        bad_response.status_code = 502
        bad_response.headers = {}

        mock_client = MagicMock()
        mock_client.__aenter__ = AsyncMock(return_value=mock_client)
        mock_client.__aexit__ = AsyncMock(return_value=None)
        mock_client.get = AsyncMock(return_value=bad_response)

        with patch("httpx.AsyncClient", return_value=mock_client):
            with pytest.raises(meme_add.DownloadServerError):
                await meme_add._download_image(
                    "https://img.example.com/a.jpg"
                )

        # api_retry 默认最多 3 次尝试
        assert mock_client.get.await_count == 3

    @pytest.mark.asyncio
    async def test_4xx_does_not_retry(self) -> None:
        """4xx 客户端错误不应重试，立即失败。"""
        from httpx import HTTPStatusError

        bad_response = MagicMock()
        bad_response.status_code = 404
        bad_response.headers = {}
        bad_response.raise_for_status.side_effect = HTTPStatusError(
            "Not Found", request=MagicMock(), response=bad_response
        )

        mock_client = MagicMock()
        mock_client.__aenter__ = AsyncMock(return_value=mock_client)
        mock_client.__aexit__ = AsyncMock(return_value=None)
        mock_client.get = AsyncMock(return_value=bad_response)

        with patch("httpx.AsyncClient", return_value=mock_client):
            with pytest.raises(HTTPStatusError):
                await meme_add._download_image(
                    "https://img.example.com/a.jpg"
                )

        assert mock_client.get.await_count == 1
```

注意：以上测试会真实触发 `api_retry` 的指数退避（5xx 成功场景约等待 1 秒，耗尽场景约等待 3 秒），在可接受范围内。如需加速，可在测试 setUp 中 patch `meme_add.api_retry` 为无等待版本，但当前保持真实行为更符合测试目的。

- [ ] **Step 3: 运行新增测试**

Run: `uv run pytest tests/unit/plugins/test_meme_add.py::TestDownloadImageRetry -v`  
Expected: 3 个测试全部通过

- [ ] **Step 4: 运行全部 `/add` 插件测试**

Run: `uv run pytest tests/unit/plugins/test_meme_add.py -v`  
Expected: 全部通过

- [ ] **Step 5: Commit**

```bash
git add tests/unit/plugins/test_meme_add.py
# 提交需经用户审核
```

---

## Task 4: 全量测试与回归检查

- [ ] **Step 1: 运行全部单元测试**

Run: `uv run pytest tests/unit/ -v`  
Expected: 全部通过

- [ ] **Step 2: 语法检查**

Run: `uv run python -m compileall bot tests`  
Expected: 无编译错误

- [ ] **Step 3: 类型检查（可选，如 pyright 可用）**

Run: `uv run pyright bot/plugins/meme_add.py`  
Expected: 无新增类型错误

---

## 自我审查

- **Spec coverage:** 设计文档中“复用 `api_retry`”“5xx 重试、4xx 不重试”“保持 30 秒超时”均已对应到 Task 1、Task 2。
- **Placeholder scan:** 无 TBD/TODO；所有代码块均提供完整代码。
- **Type consistency:** `DownloadServerError` 在实现和测试中命名一致；`_download_image` 签名不变。
